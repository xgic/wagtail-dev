# Producer Compose. App starters use https://github.com/xgic/wagtail.
# Builds and tags ghcr.io/xgic/wagtail-dev FROM official Python/Wagtail
# pins. GHCR push is the Publish GHCR workflow after a v* tag.
name: xgic-wagtail-dev

services:
  xgic-wagtail-dev:
    image: ghcr.io/xgic/wagtail-dev:0.1.2
    build:
      context: ..
      dockerfile: Dockerfile
    # Root so the image entrypoint can align the docker-sock GID, then
    # exec CMD as vscode. Do not enable Docker-in-Docker.
    user: "0:0"
    container_name: xgic-wagtail-dev
    working_dir: /workspace
    command: ["sleep", "infinity"]
    environment:
      PYTHONDONTWRITEBYTECODE: "1"
      PYTHONUNBUFFERED: "1"
      XGIC_COMPOSE_FILE: .devcontainer/docker-compose.yml
      XGIC_COMPOSE_PROJECT: xgic-wagtail-dev
      XGIC_PRIMARY_SERVICE: xgic-wagtail-dev
    networks:
      - app-network
    ports:
      - "8000:8000"
    volumes:
      - ..:/workspace:cached
      - /var/run/docker.sock:/var/run/docker.sock
    depends_on:
      postgres:
        condition: service_healthy
    stop_grace_period: 5s

  postgres:
    image: postgres:18-bookworm
    container_name: xgic-wagtail-dev-postgres
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-wagtail}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-wagtail}
      POSTGRES_DB: ${POSTGRES_DB:-wagtail}
    networks:
      - app-network
    volumes:
      # postgres:18+ stores data under a major-version subdirectory.
      - postgres-data:/var/lib/postgresql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB"]
      interval: 5s
      timeout: 5s
      retries: 10
    stop_grace_period: 10s

networks:
  app-network:

volumes:
  postgres-data:
